{% include ('PartialFiles/_head.html.twig') %}

<body class="bodyAdmin">


        <main class="adminMainList">

                <h1 class="adminListTitle">Liste des NEWS</h1>

                {% for news in news %}

                        <p class="checkerboardList">{{ news.title }}</p>
                        <a class="checkerboardListModify" href="{{ path('admin_news_modify', {'id': news.id}) }}">  Modifier  </a>
                        <a class="checkerboardListDelete" href="{{ path('admin_news_delete', {'id': news.id}) }}">  Supprimer  </a>

                {% endfor %}


        </main>


</body>

</html>